<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_clinic_patient_tree" model="ir.ui.view">
        <field name="name">clinic.patient.tree</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="date_of_birth"/>
                <field name="gender"/>
                <field name="phone"/>
                <field name="email"/>
                <field name="age"/>
            </tree>
        </field>
    </record>

    <record id="view_clinic_patient_form" model="ir.ui.view">
        <field name="name">clinic.patient.form</field>
        <field name="model">clinic.patient</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="date_of_birth"/>
                        <field name="gender"/>
                        <field name="age" readonly="1"/>
                        <field name="phone"/>
                        <field name="email"/>
                        <field name="address"/>
                        <field name="medical_history"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_clinic_patient" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">clinic.patient</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="clinic_root_menu" name="Clinic"/>
    <menuitem id="clinic_patient_menu" name="Patients" parent="clinic_root_menu" action="action_clinic_patient"/>
</odoo>

